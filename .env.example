# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# OpenAI (agentes)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Email (Fase 1+)
RESEND_API_KEY=
RESEND_FROM="JSM Flow <noreply@jsm.test>"
NOTIFY_ADMIN_EMAIL=
NOTIFY_CONTABILIDAD_EMAIL=        # Fase 8: avisos a Contabilidad (sube a portal / PO CONFIRMA)

# WhatsApp (Fase 4+)
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
WHATSAPP_VERIFY_TOKEN=

# Cron / jobs (Fase 4): protege el endpoint de cobranza
CRON_SECRET=

# Seguridad: intentos de login por minuto por IP (default 8). Subir solo en dev/e2e.
LOGIN_MAX_PER_MIN=
